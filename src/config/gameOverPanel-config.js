export const dimensions = {
  TEXT_X: 0,
  TEXT_Y: 0,
  TEXT_WIDTH: 191,
  TEXT_HEIGHT: 11,
  RESTART_WIDTH: 36,
  RESTART_HEIGHT: 32
};

export const spriteIds = {
  LDPI: {
      GAME_OVER: 'gameOver-1x',
      RESTART: 'restart-1x'
  },
  HDPI: {
      GAME_OVER: 'gameOver-2x',
      RESTART: 'restart-2x'
  }
};